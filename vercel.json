{
  "buildCommand": "npm run generate:manifest && node scripts/generate-thumbnails-safe.js && next build",
  "installCommand": "npm ci --include=optional",
  "framework": "nextjs",
  "env": {
    "STATIC_EXPORT": "false",
    "NODE_ENV": "production",
    "NEXT_PUBLIC_ENABLE_VERCEL_ANALYTICS": "1"
  },
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  }
}

