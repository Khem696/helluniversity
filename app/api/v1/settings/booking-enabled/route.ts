/**
 * Booking Settings API v1
 * 
 * Versioned endpoint for booking settings
 * Maintains backward compatibility with /api/settings/booking-enabled
 * 
 * GET /api/v1/settings/booking-enabled - Check if bookings are enabled
 */

export { GET } from '../../../settings/booking-enabled/route'

