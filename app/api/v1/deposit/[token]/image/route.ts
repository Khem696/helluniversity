/**
 * Deposit Image API v1
 * 
 * Versioned endpoint for deposit image access
 * Maintains backward compatibility with /api/deposit/[token]/image
 * 
 * GET /api/v1/deposit/[token]/image - Get deposit image by token
 */

export { GET } from '../../../deposit/[token]/image/route'

